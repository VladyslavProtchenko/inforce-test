import React from 'react';

const Loader = () => {
    return (
        <div className="flex w-max flex-auto items-center justify-center">
            Loading data...
        </div>
    );
};

export default Loader;